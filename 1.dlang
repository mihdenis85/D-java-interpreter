var factorial := func (n) is
    if n = 0 then
        return 1;
    else
        var newValue := n - 1;
        return n * factorial(newValue);
    end;
end;

print factorial(5); // Should print 120
