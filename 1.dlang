var sum := func (a, b) is
  return a + b;
end;

var mult := func (a, b) is
  return a * b;
end;

var num1 := readInt();
var num2 := readInt();

if num1 < 0 or num2 < 0 then
  print mult(num1, num2);
else
  print add(num1, num2);
end;
